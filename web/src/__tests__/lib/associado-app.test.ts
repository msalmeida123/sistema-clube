import {hashSenha,confereSenha,cpfSchema,relatoSchema,origemValida,digest} from '@/lib/associado-app'
import {NextRequest} from 'next/server'
test('senha é salgada e não é armazenada em texto puro',async()=>{const hash=await hashSenha('senha-de-teste-123');expect(hash).not.toContain('senha-de-teste');expect(await confereSenha('senha-de-teste-123',hash)).toBe(true);expect(await confereSenha('outra',hash)).toBe(false);expect(await hashSenha('senha-de-teste-123')).not.toBe(hash)})
test('CPF aceita máscara e recusa tamanho inválido',()=>{expect(cpfSchema.parse('123.456.789-01')).toBe('12345678901');expect(cpfSchema.safeParse('1').success).toBe(false)})
test('relato não permite definir pessoa, status nem penalidade',()=>{const r=relatoSchema.parse({id:'10000000-0000-4000-8000-000000000099',assunto:'Ocorrência',descricao:'Descrição de teste da ocorrência',local:'Piscina',data_ocorrencia:'2020-01-01',associado_id:'outra-pessoa',status:'encerrado',penalidade:'suspensao'});expect(r).not.toHaveProperty('associado_id');expect(r).not.toHaveProperty('status');expect(r).not.toHaveProperty('penalidade')})
test('recusa origem externa e aceita a origem do app',()=>{delete process.env.APP_PUBLIC_URL;expect(origemValida(new NextRequest('http://localhost:3000/api/associado-app/auth',{headers:{origin:'http://outro.test'}}))).toBe(false);expect(origemValida(new NextRequest('http://localhost:3000/api/associado-app/auth',{headers:{origin:'http://localhost:3000'}}))).toBe(true)})
test('identificador de sessão é persistido como hash',()=>{expect(digest('segredo')).toHaveLength(64);expect(digest('segredo')).not.toContain('segredo')})
