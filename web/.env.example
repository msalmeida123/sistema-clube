# ==============================================
# SISTEMA CLUBE - Variáveis de Ambiente
# ==============================================
# Copie este arquivo para .env.local e preencha os valores
# NUNCA commite o arquivo .env.local no Git!

# ==============================================
# SUPABASE (Obrigatório)
# ==============================================
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sua-chave-anonima-aqui

# Chave de serviço - APENAS para operações server-side
# NUNCA exponha esta chave no frontend!
SUPABASE_SERVICE_ROLE_KEY=sua-chave-de-servico-aqui

# ==============================================
# SEGURANÇA (Recomendado)
# ==============================================

# Secret do Webhook WaSender
# Gere uma chave aleatória: openssl rand -hex 32
# Configure a mesma chave no painel do WaSender
WASENDER_WEBHOOK_SECRET=sua-chave-secreta-do-webhook

# ==============================================
# INTEGRAÇÕES EXTERNAS (Opcional)
# ==============================================

# WaSender API (configurado no banco de dados)
# A API key do WaSender é armazenada na tabela config_wasender

# OpenAI API (configurado no banco de dados)
# A API key da OpenAI é armazenada na tabela config_bot_ia

# ==============================================
# AMBIENTE
# ==============================================
NODE_ENV=development
# Em produção, altere para: NODE_ENV=production

# ==============================================
# NOTAS DE SEGURANÇA
# ==============================================
# 
# 1. Nunca exponha SUPABASE_SERVICE_ROLE_KEY no frontend
# 2. Configure WASENDER_WEBHOOK_SECRET para proteger o webhook
# 3. Use senhas fortes e únicas para cada ambiente
# 4. Rotacione as chaves periodicamente
# 5. Monitore os logs de webhook para atividades suspeitas
#
# Para gerar chaves seguras:
# - Linux/Mac: openssl rand -hex 32
# - Online: https://generate-secret.vercel.app/32
#
