const fs=require('fs'),cp=require('child_process');const env=Object.fromEntries(fs.readFileSync('deploy/supabase-local/.env','utf8').split(/\r?\n/).filter(x=>x.includes('=')).map(x=>[x.slice(0,x.indexOf('=')),x.slice(x.indexOf('=')+1)]));
const code="const {createClient}=require('@supabase/supabase-js'); const {executarRetencao}=require('./workers/retencao'); executarRetencao(createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.SUPABASE_SERVICE_ROLE_KEY,{auth:{persistSession:false,autoRefreshToken:false}})).then(()=>console.log('PASS: imagem de retencao executou sem pendencias')).catch(()=>process.exit(1));";
const r=cp.spawnSync('docker',['run','--rm','--network','clube-supabase-local','-e','NEXT_PUBLIC_SUPABASE_URL=http://api-gw:8000','-e','SUPABASE_SERVICE_ROLE_KEY='+env.SERVICE_ROLE_KEY,'sistema-clube:worker-retencao-20260908','node','node_modules/tsx/dist/cli.mjs','-e',code],{encoding:'utf8'});console.log(r.stdout);if(r.status)console.error('Falha ao iniciar imagem de retenção');process.exitCode=r.status;
